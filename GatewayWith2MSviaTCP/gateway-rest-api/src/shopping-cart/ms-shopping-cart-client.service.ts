import { Inject, Injectable } from '@nestjs/common';
import { ClientProxy } from '@nestjs/microservices';

@Injectable()
export class MsShoppingCartClient {
    
    //constructor(@Inject('GATEWAY-REST-API') private readonly client: ClientProxy) { };



}
